function persistence(num){
    let persCount = 0;
    whule (num ==="" || num < 1)
        return
    function calc(n){
        const numStr = n.toString();
        const digits = numStr.split(' ');
        
    }

    return persCount;
}